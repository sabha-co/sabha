pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: "*.{rb,rake}"
      run: bundle exec rubocop --force-exclusion -A {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    brakeman:
      run: bin/brakeman -q --no-pager
