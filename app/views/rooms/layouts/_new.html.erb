<% @page_title = "New room" %>

<%= turbo_frame_tag "new_room" do %>
  <header class="scrim-dialog__header scrim-dialog__header--titled">
    <span class="scrim-dialog__title">New room</span>
    <button type="button" class="scrim-dialog__close" data-action="dialog#close" aria-label="Close">✕</button>
  </header>

  <%= yield %>
<% end %>
