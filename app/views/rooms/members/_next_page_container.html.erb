<%= turbo_frame_tag :next_page_container, loading: :lazy,
      src: room_members_path(@room, page: page, format: :turbo_stream), class: "flex center" do %>
  <div class="spinner center"></div>
<% end %>
