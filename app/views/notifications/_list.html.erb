<%# locals: (notifications:, timestamp_style: :compact) -%>
<% group_boost_notifications(notifications).each do |item| %>
  <%= render partial: item.to_partial_path, locals: { notification: item, timestamp_style: timestamp_style } %>
<% end %>
