<div class="demo-banner">
  <div class="demo-banner__inner">
    <strong>Demo Mode</strong>
    <span>This server resets <%= DemoMode.reset_interval %>. Data is not persisted.</span>
  </div>
</div>

<style>
  .demo-banner__inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: var(--color-warning, #f59e0b);
    color: var(--color-warning-contrast, #000);
    font-size: 0.875rem;
    text-align: center;
  }

  .demo-banner strong {
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
</style>
