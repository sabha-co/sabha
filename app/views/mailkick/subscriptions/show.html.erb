<!DOCTYPE html>
<html>
  <head>
    <title><%= Branding.contextual_app_name %> Notifications</title>

    <style>
      body {
        padding: 20px;
        font-family: "Helvetica Neue", Arial, Helvetica, sans-serif;
        font-size: 14px;
        line-height: 1.4;
      }

      a {
        color: blue;
      }

      .container {
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <% if !subscribed? %>
        <p>You are unsubscribed from <%= Branding.contextual_app_name %> notifications.</p>
        <p><%= link_to "Resubscribe", subscribe_url %></p>
      <% else %>
        <p>You are subscribed to <%= Branding.contextual_app_name %> notifications.</p>
        <p><%= link_to "Unsubscribe", unsubscribe_url %></p>
      <% end %>
    </div>
  </body>
</html>
