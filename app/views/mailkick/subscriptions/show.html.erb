<!DOCTYPE html>
<html>
  <head>
    <title>Small Bets Notifications</title>

    <style>
      body {
        padding: 20px;
        font-family: "Helvetica Neue", Arial, Helvetica, sans-serif;
        font-size: 14px;
        line-height: 1.4;
      }

      a {
        color: blue;
      }

      .container {
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <% if !subscribed? %>
        <p>You are unsubscribed from Small Bets notifications.</p>
        <p><%= link_to "Resubscribe", subscribe_url %></p>
      <% else %>
        <p>You are subscribed to Small Bets notifications.</p>
        <p><%= link_to "Unsubscribe", unsubscribe_url %></p>
      <% end %>
    </div>
  </body>
</html>
