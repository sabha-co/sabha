<%# locals: (user:) %>
<% if user.administrator? %>
  <span class="txt-x-small txt-muted" title="Administrator"><%= icon_tag "crown" %></span>
<% elsif user.moderator? %>
  <span class="txt-x-small txt-muted" title="Moderator"><%= icon_tag "shield" %></span>
<% elsif user.bot? %>
  <span class="txt-x-small txt-muted" title="Bot"><%= icon_tag "bot" %></span>
<% end %>
<% if user.badge %>
  <%= badge_tag user.badge, class_name: "txt-x-small" %>
<% end %>
