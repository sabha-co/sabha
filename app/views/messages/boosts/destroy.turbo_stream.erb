<%# Removing a boost may drop an avatar from a chip or remove the chip entirely;
    replace the whole grouped container to reflect either. %>
<%= turbo_stream.replace_all "[id='#{dom_id(@message, :boosting)}']",
      partial: "messages/boosts/boosts", locals: { message: @message } %>
