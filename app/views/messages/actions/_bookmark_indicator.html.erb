<%# locals: (message:) -%>
<span id="<%= dom_id(message, :bookmark_indicator) %>">
  <% if message.bookmarked_by?(Current.user) %>
    <%= icon_tag "bookmark-filled", class: "translucent bookmark-indicator--active", title: "Bookmarked" %>
  <% end %>
</span>
