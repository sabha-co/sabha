log/
/storage/
tmp/

# Schema cache drifts whenever SaaS provisioning regenerates it from a
# tenant DB. Regenerate locally with `bin/rails db:schema:cache:dump`.
db/schema_cache.yml

# SaaS mode marker file (use SAAS=true env var in production)
tmp/saas.txt

# Workspace databases (SaaS mode)
storage/workspaces/
storage/untenanted/

# Build artifacts generated by Tailwind CLI
app/assets/builds/
public/assets/
node_modules/
.pnpm-store/
pnpm-debug.log*
bun.lock

# Ignore IDE-specific files and directories
.idea/
.vscode/
*.sublime-workspace

# Ignore RVM-based gemset selection
.ruby-gemset
.bundle/
.env
.env.multitenant
dump.rdb

# Ignore master key for decrypting credentials (if used)
/config/master.key

# Ignore local GitHub Actions files
.github/.env

.DS_Store

node_modules

# Host-specific Caddy configuration
Caddyfile

.worktrees
.agents/
.claude/

# Claude Code local settings (user-specific paths and permissions)
.claude/settings.local.json
